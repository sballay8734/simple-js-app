    // function find(types) {
    //     pokemonRepository.getAll().forEach(element => {
    //         if (types.sort() === element.types.sort()) {
    //             return element;
    //         } else {
    //             return;
    //         };
    //     });
    // }